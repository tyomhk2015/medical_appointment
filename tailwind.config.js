/** @type {import('tailwindcss').Config} */
module.exports = {
  // Delcaring using Tailwind CSS feature in the following files.
  // When multiple files are designated, spaces between file extensions will cause an error.
  content: ["./pages/**/*.tsx", "./components/**/*.tsx"],
  theme: {
    extend: {},
  },
  plugins: [],
}
